<div class="product">
    <div class="product__image-block">
        <img src={{this.product.image}} alt="product image" class="product__image">
    </div>
    <div class="product__description">
        <div class="product__name">
            {{this.product.name}}
        </div>
        <div class="product__price">
            {{#if this.productWithDiscount}}
                {{this.productWithDiscount.price}}
            {{/if}}
            £{{this.product.price}}
            <QuantityCounter @quantity={{this.quantity}} @changeQuantity={{this.changeQuantity}} />
            <Button {{on "click" this.addToCart}}>Buy</Button>
        </div>
    </div>
</div>
